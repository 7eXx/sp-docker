# Service Provider Docker
This is an easy application to test the SAML 2.0 protocol.

## How to run

```bash
$ docker composet up -d
```

Then you can access to the application at http://localhost:8080  
If everything works correctly you should see metadata information.
